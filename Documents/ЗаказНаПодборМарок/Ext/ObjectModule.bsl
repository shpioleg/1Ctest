
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	ЕстьПолныйПодборМарок = Истина;
	Для каждого стр из Товары Цикл
		Если НЕ ЗначениеЗаполнено(стр.КлючСтроки) Тогда
			стр.КлючСтроки = стр.НомерСтроки;	
		КонецЕсли;
		Если ЕстьПолныйПодборМарок И НЕ (стр.Количество = стр.КоличествоПодобрано) Тогда
			ЕстьПолныйПодборМарок = Ложь;	
		КонецЕсли;		
	КонецЦикла;
	МаркиПолностьюПодобраны = ЕстьПолныйПодборМарок;
КонецПроцедуры
